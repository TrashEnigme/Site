function Login(){
	var done=0;
	var username=document.login.username.value;
	var password=document.login.password.value;

	if (username=="Trash" && password=="Myst") {
		window.location="site.html";
	}

	if (username=="KyaBoomy" && password=="EhRegardePasLeCodeSTP") { 
		window.location="site.html";
	}
}